services:
  # MySQL Database
  mysql:
    image: mysql:8.0
    container_name: sgaiti-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-sgaiti_root_pass}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-sgaiti_db}
      MYSQL_USER: ${MYSQL_USER:-sgaiti_user}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-sgaiti_pass}
    ports:
      - "${MYSQL_HOST_PORT:-53106}:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - sgaiti-network
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10

  # phpMyAdmin
  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    container_name: sgaiti-phpmyadmin
    restart: always
    environment:
      PMA_HOST: mysql
      PMA_PORT: 3306
      PMA_USER: ${MYSQL_USER:-sgaiti_user}
      PMA_PASSWORD: ${MYSQL_PASSWORD:-sgaiti_pass}
      UPLOAD_LIMIT: 50M
    ports:
      - "${PHPMYADMIN_HOST_PORT:-58090}:80"
    depends_on:
      mysql:
        condition: service_healthy
    networks:
      - sgaiti-network

  # Laravel + Inertia Application
  sgaiti:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: sgaiti-app
    restart: always
    ports:
      - "${APP_HOST_PORT:-8000}:8000"
    depends_on:
      mysql:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - storage_data:/app/storage
    environment:
      - DB_CONNECTION=mysql
      - DB_HOST=mysql
      - DB_PORT=3306
      - DB_DATABASE=${MYSQL_DATABASE:-sgaiti_db}
      - DB_USERNAME=${MYSQL_USER:-sgaiti_user}
      - DB_PASSWORD=${MYSQL_PASSWORD:-sgaiti_pass}
    networks:
      - sgaiti-network

volumes:
  mysql_data:
  storage_data:

networks:
  sgaiti-network:
    driver: bridge
